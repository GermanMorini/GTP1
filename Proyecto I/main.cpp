#include<iostream>
//#include "fileManager.h"
#include "structures.h"

using namespace std;

/*
void parse_opt(int o) {
        switch(o) {
                case 1:
                        Wine w = menu() ...
                        dbManager.addProduct(w);
                        break;
                case 2:
                        Wine w = menu() ...
                        dbManager.delProduct(w);
                        break;
                case 3:
                        ProviderData pd = menu() ...
                        dbManager.search(pd);
                        break;
                case 4:
                        Wine w = menu() ...
                        dbManager.search(w);
                        break;
                case 5:

        }
}

int input() {
        // validación de la opción elegida
        return ...
}

void start_menu() {
        while(true) {
                parse_opt(input());
        }
}


*/

int main() {
        // start_menu();
        return 0;
}
